import React from "react";
import ReactDOM from "react-dom/client";

type AppView = "landing" | "flow";
type FlowStep = "ask" | "shuffle" | "draw" | "observe" | "reflect";

type Card = { name: string; meaning: string; imageUrl: string };

type ObservationAnswers = { first: string; attention: string; associations: string };

const CARDS: Card[] = [
  { name: "The Fool", meaning: "A beginning is asking to be trusted.", imageUrl: "https://www.sacred-texts.com/tarot/pkt/img/ar00.jpg" },
  { name: "The Magician", meaning: "You already have more agency than you think.", imageUrl: "https://www.sacred-texts.com/tarot/pkt/img/ar01.jpg" },
  { name: "The High Priestess", meaning: "What you need may be quieter than what you want.", imageUrl: "https://www.sacred-texts.com/tarot/pkt/img/ar02.jpg" },
  { name: "Strength", meaning: "Gentleness is also a form of power.", imageUrl: "https://www.sacred-texts.com/tarot/pkt/img/ar08.jpg" },
  { name: "The Hermit", meaning: "A little solitude may clarify what matters.", imageUrl: "https://www.sacred-texts.com/tarot/pkt/img/ar09.jpg" },
  { name: "Temperance", meaning: "Balance is found by blending, not forcing.", imageUrl: "https://www.sacred-texts.com/tarot/pkt/img/ar14.jpg" },
  { name: "The Star", meaning: "Hope returns when you stop demanding certainty.", imageUrl: "https://www.sacred-texts.com/tarot/pkt/img/ar17.jpg" },
  { name: "The Moon", meaning: "Your uncertainty is telling the truth about your fear.", imageUrl: "https://www.sacred-texts.com/tarot/pkt/img/ar18.jpg" },
];

function App() {
  const [view, setView] = React.useState<AppView>(window.location.pathname === "/app" ? "flow" : "landing");
  const [step, setStep] = React.useState<FlowStep>("ask");
  const [question, setQuestion] = React.useState("");
  const [suggestion, setSuggestion] = React.useState("");
  const [deck, setDeck] = React.useState<Card[]>([]);
  const [chosenCard, setChosenCard] = React.useState<Card | null>(null);
  const [selectedCardIndex, setSelectedCardIndex] = React.useState<number | null>(null);
  const [observations, setObservations] = React.useState<ObservationAnswers>({ first: "", attention: "", associations: "" });
  const [isShuffling, setIsShuffling] = React.useState(false);
  const [shufflePhase, setShufflePhase] = React.useState<"idle" | "mix" | "settle">("idle");

  React.useEffect(() => {
    const onPopState = () => setView(window.location.pathname === "/app" ? "flow" : "landing");
    window.addEventListener("popstate", onPopState);
    return () => window.removeEventListener("popstate", onPopState);
  }, []);

  const navigate = (to: string) => {
    window.history.pushState({}, "", to);
    setView(to === "/app" ? "flow" : "landing");
  };

  if (view === "flow") {
    return (
      <MirrorFlow
        step={step}
        setStep={setStep}
        question={question}
        setQuestion={setQuestion}
        suggestion={suggestion}
        setSuggestion={setSuggestion}
        deck={deck}
        setDeck={setDeck}
        chosenCard={chosenCard}
        setChosenCard={setChosenCard}
        selectedCardIndex={selectedCardIndex}
        setSelectedCardIndex={setSelectedCardIndex}
        observations={observations}
        setObservations={setObservations}
        isShuffling={isShuffling}
        setIsShuffling={setIsShuffling}
        shufflePhase={shufflePhase}
        setShufflePhase={setShufflePhase}
        onBack={() => navigate("/")}
      />
    );
  }

  return <LandingPage onEnter={() => navigate("/app")} />;
}

function LandingPage({ onEnter }: { onEnter: () => void }) {
  React.useEffect(() => {
    const observer = new IntersectionObserver((entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          entry.target.classList.add("visible");
          observer.unobserve(entry.target);
        }
      });
    }, { threshold: 0.07, rootMargin: "0px 0px -48px 0px" });
    document.querySelectorAll(".reveal").forEach((el) => observer.observe(el));
    return () => observer.disconnect();
  }, []);

  return (
    <>
      <style>{`:root { color-scheme: light; --cream:#f8f4ef; --cream-mid:#f2ede5; --ink:#1c1916; --ink-soft:#4a4540; --ink-muted:#9a9088; --blush:#d4afa8; --lavender:#c2bcd8; --gold:#c8a96e; --line:rgba(28,25,22,0.08); --line-soft:rgba(28,25,22,0.05); --glass-bg:rgba(255,255,255,0.55); --serif:'Cormorant Garamond', Georgia, serif; --sans:'DM Sans', system-ui, sans-serif; --mono:'DM Mono', monospace; } * { box-sizing:border-box; margin:0; padding:0; } html { scroll-behavior:smooth; } body { background:var(--cream); color:var(--ink); font-family:var(--sans); font-size:16px; line-height:1.75; overflow-x:hidden; } a { color:inherit; text-decoration:none; } body::before { content:''; position:fixed; top:0; left:72px; width:1px; height:100vh; background:linear-gradient(to bottom, transparent, var(--line) 15%, var(--line) 85%, transparent); pointer-events:none; z-index:10; } .container { max-width:860px; margin:0 auto; padding:0 56px 0 128px; } .container--wide { max-width:1080px; margin:0 auto; padding:0 56px 0 128px; } section { padding:140px 0; } section + section { border-top:1px solid var(--line-soft); } .eyebrow { font-family:var(--mono); font-size:10px; letter-spacing:0.15em; text-transform:uppercase; color:var(--ink-muted); margin-bottom:40px; display:flex; align-items:center; gap:16px; } .eyebrow::before { content:''; width:20px; height:1px; background:var(--blush); flex-shrink:0; } h1 { font-family:var(--serif); font-size:clamp(60px, 8vw, 104px); font-weight:300; line-height:1; letter-spacing:-0.01em; color:var(--ink); margin-bottom:28px; } h2 { font-family:var(--serif); font-size:clamp(34px, 4.5vw, 54px); font-weight:300; line-height:1.1; letter-spacing:-0.01em; margin-bottom:36px; } h2 em { font-style:italic; color:var(--ink-soft); } p { font-size:16px; color:var(--ink-soft); line-height:1.85; margin-bottom:22px; } .hero-subtitle { font-family:var(--serif); font-size:clamp(19px, 2.4vw, 26px); font-weight:300; font-style:italic; color:var(--ink-soft); margin-bottom:24px; line-height:1.6; } .hero-supporting { font-size:16px; color:var(--ink-muted); margin-bottom:56px; } .pull-quote { font-family:var(--serif); font-size:clamp(20px, 2.6vw, 28px); font-weight:300; font-style:italic; line-height:1.55; color:var(--ink); border-left:2px solid var(--blush); padding-left:28px; margin:0; } .two-col { display:grid; grid-template-columns:1fr 1fr; gap:80px; align-items:start; } .btn-primary, .btn-ghost { display:inline-flex; flex-direction:column; align-items:flex-start; gap:6px; padding:20px 40px; border-radius:3px; font-family:var(--sans); font-size:14px; letter-spacing:0.04em; cursor:pointer; transition: background .2s, transform .2s, border-color .2s; } .btn-primary { background:var(--ink); color:var(--cream); border:none; } .btn-ghost { background:transparent; color:var(--ink); border:1px solid var(--line); } .btn-primary:hover, .btn-ghost:hover { transform: translateY(-2px); } .hero-btns { display:flex; gap:16px; flex-wrap:wrap; } #hero { padding-top:160px; padding-bottom:160px; position:relative; overflow:hidden; background:var(--cream); } .orb { position:absolute; border-radius:50%; pointer-events:none; } .orb-lav { top:-100px; right:-180px; width:720px; height:720px; background: radial-gradient(circle, rgba(194,188,216,0.18) 0%, transparent 62%); } .orb-blush { bottom:-40px; left:-120px; width:440px; height:440px; background: radial-gradient(circle, rgba(212,175,168,0.12) 0%, transparent 65%); } #preview { background: linear-gradient(180deg, var(--cream) 0%, rgba(237,234,245,0.4) 50%, var(--cream) 100%); padding:120px 0; } .preview-intro { text-align:center; margin-bottom:80px; } .preview-intro .eyebrow { justify-content:center; } .preview-intro .eyebrow::before { display:none; } .preview-intro h2 { max-width:480px; margin:0 auto 16px; } .preview-intro p { max-width:400px; margin:0 auto; font-size:15px; } .phones-row { display:grid; grid-template-columns:repeat(3, 1fr); gap:32px; align-items:end; max-width:900px; margin:0 auto; padding:0 56px 0 128px; } .phone-wrap { display:flex; flex-direction:column; align-items:center; gap:20px; } .phone-wrap:nth-child(2) { transform:translateY(-40px); } .phone-label { font-family:var(--mono); font-size:10px; letter-spacing:0.12em; text-transform:uppercase; color:var(--ink-muted); } .phone-shell { width:100%; border-radius:44px; background:#1a1714; padding:14px; box-shadow:0 40px 80px rgba(28,25,22,0.16), 0 8px 24px rgba(28,25,22,0.10), inset 0 1px 0 rgba(255,255,255,0.08); } .phone-screen { border-radius:32px; overflow:hidden; width:100%; } .phone-screen svg { display:block; width:100%; } #what { background:var(--cream); } #how { background: linear-gradient(180deg, var(--cream) 0%, rgba(245,236,234,0.35) 100%); } .flow { display:flex; flex-direction:column; } .flow-step { display:flex; gap:32px; align-items:flex-start; padding:32px 0; border-bottom:1px solid var(--line-soft); } .flow-step:last-child { border-bottom:none; } .flow-num { font-family:var(--mono); font-size:10px; color:var(--blush); letter-spacing:0.08em; flex-shrink:0; padding-top:4px; width:24px; } .flow-label { font-family:var(--serif); font-size:21px; font-weight:400; color:var(--ink); line-height:1.3; } .flow-desc { font-size:14px; color:var(--ink-muted); margin-top:5px; line-height:1.65; } .medium-chips { display:flex; flex-wrap:wrap; gap:8px; margin-top:20px; } .chip { font-family:var(--mono); font-size:10px; letter-spacing:0.08em; color:var(--ink-soft); border:1px solid var(--line); padding:8px 16px; border-radius:2px; background:var(--glass-bg); } #lens { background: linear-gradient(180deg, var(--cream) 0%, rgba(237,234,245,0.5) 40%, rgba(245,236,234,0.45) 75%, var(--cream) 100%); position:relative; overflow:hidden; } #lens::before { content:''; position:absolute; top:-200px; left:50%; transform:translateX(-50%); width:900px; height:600px; border-radius:50%; background: radial-gradient(ellipse, rgba(194,188,216,0.12) 0%, transparent 65%); pointer-events:none; } .lens-intro { text-align:center; margin-bottom:80px; } .lens-intro .eyebrow { justify-content:center; } .lens-intro .eyebrow::before { display:none; } .lens-intro h2 { max-width:520px; margin:0 auto 20px; } .lens-card-wrap { max-width:560px; margin:0 auto; filter: drop-shadow(0 24px 60px rgba(28,25,22,0.08)) drop-shadow(0 4px 16px rgba(28,25,22,0.05)); } .lens-card-wrap svg { display:block; width:100%; border-radius:20px; } #founder { background:var(--cream); } #founding { background:var(--cream-mid); } .benefits { list-style:none; margin:44px 0 52px; } .benefits li { display:flex; align-items:flex-start; gap:18px; padding:18px 0; border-bottom:1px solid var(--line-soft); font-size:15px; color:var(--ink-soft); } .benefits li:first-child { border-top:1px solid var(--line-soft); } .benefits li::before { content:'✓'; font-family:var(--mono); font-size:11px; color:var(--blush); flex-shrink:0; padding-top:3px; } .founding-price { font-family:var(--serif); font-size:36px; font-weight:300; color:var(--ink); margin-bottom:6px; } .founding-price-note { font-family:var(--mono); font-size:11px; color:var(--ink-muted); letter-spacing:0.08em; margin-bottom:32px; } footer { background:var(--ink); border-top:1px solid rgba(247,243,238,0.07); padding:44px 0; } .footer-inner { max-width:860px; margin:0 auto; padding:0 56px 0 128px; display:flex; align-items:center; justify-content:space-between; } .footer-logo { font-family:var(--serif); font-size:18px; color:rgba(247,243,238,0.50); } .footer-note { font-family:var(--mono); font-size:10px; color:rgba(247,243,238,0.25); letter-spacing:0.06em; } .reveal { opacity:0; transform: translateY(24px); transition: opacity .8s ease, transform .8s ease; } .reveal.visible { opacity:1; transform:none; } @media (max-width:860px) { body::before { left:24px; } .container, .container--wide { padding:0 28px 0 60px; } .footer-inner { padding:0 28px 0 60px; } .two-col { grid-template-columns:1fr; } .phones-row { grid-template-columns:1fr; padding:0 28px; gap:40px; } .phone-wrap:nth-child(2) { transform:none; } section { padding:96px 0; } .hero-btns { flex-direction:column; } } @media (prefers-reduced-motion:reduce) { .reveal { opacity:1; transform:none; transition:none; } }
      `}</style>
      <section id="hero"><div className="orb orb-lav" /><div className="orb orb-blush" /><div className="container" style={{ position:'relative', zIndex:1 }}><h1>Mirror</h1><p className="hero-subtitle"><span>We don't live in the world itself.<br />We live in our interpretation of it.</span></p><p className="hero-supporting"><span>Observe the stories shaping your life.</span></p><div className="hero-btns"><button type="button" className="btn-ghost" onClick={onEnter}><span>Enter Mirror</span></button><a href="https://www.creem.io/payment/prod_6oltU4M6Pxwj6JFSQbVHZQ" className="btn-primary" target="_blank" rel="noreferrer"><span>Become a Founding Member</span><span className="btn-sub"><span>$2 Early Access · Lifetime included</span></span></a></div></div></section>
      <section id="preview"><div className="container--wide"><div className="preview-intro reveal"><p className="eyebrow"><span>Inside Mirror</span></p><h2><span>A glimpse inside<br /><em>Mirror</em></span></h2><p><span>Not a Tarot app. A space to observe how you interpret reality.</span></p></div><div className="phones-row reveal"><div className="phone-wrap"><div className="phone-shell"><div className="phone-screen"><svg viewBox="0 0 300 580" xmlns="http://www.w3.org/2000/svg"><rect width="300" height="580" fill="#f5ecea" /><text x="24" y="32" fontFamily="DM Sans,sans-serif" fontSize="11" fill="#9a9088">9:41</text><text x="150" y="72" fontFamily="Cormorant Garamond,serif" fontSize="18" fill="#1c1916" textAnchor="middle" letterSpacing="1">Mirror</text><rect x="24" y="104" width="228" height="66" rx="14" fill="rgba(255,255,255,0.72)" /><text x="36" y="124" fontFamily="DM Mono,monospace" fontSize="9" fill="#9a9088" letterSpacing="1">YOUR QUESTION</text><text x="36" y="146" fontFamily="DM Sans,sans-serif" fontSize="13" fill="#1c1916">Should I leave law school?</text><text x="36" y="162" fontFamily="DM Sans,sans-serif" fontSize="12" fill="#6a6058">I feel trapped.</text></svg></div></div><span className="phone-label">Question</span></div><div className="phone-wrap"><div className="phone-shell"><div className="phone-screen"><svg viewBox="0 0 300 580" xmlns="http://www.w3.org/2000/svg"><rect width="300" height="580" fill="#edeaf5" /><text x="150" y="72" fontFamily="Cormorant Garamond,serif" fontSize="18" fill="#1c1916" textAnchor="middle">Mirror</text><rect x="90" y="108" width="120" height="184" rx="12" fill="white" /><text x="150" y="263" fontFamily="Cormorant Garamond,serif" fontSize="11" fill="#4a4540" textAnchor="middle" letterSpacing="2">THE MOON</text></svg></div></div><span className="phone-label">Draw</span></div><div className="phone-wrap"><div className="phone-shell"><div className="phone-screen"><svg viewBox="0 0 300 580" xmlns="http://www.w3.org/2000/svg"><rect width="300" height="580" fill="#f5ecea" /><text x="24" y="114" fontFamily="DM Mono,monospace" fontSize="9" fill="#c2bcd8" letterSpacing="2">INTERPRETATION LENS</text><text x="150" y="174" fontFamily="DM Sans,sans-serif" fontSize="12" fill="#2a2520" textAnchor="middle" fontWeight="500">Waiting for permission</text></svg></div></div><span className="phone-label">Interpretation Lens</span></div></div></div></section>
      <section id="what"><div className="container"><div className="two-col reveal"><div><p className="eyebrow"><span>What is Mirror</span></p><h2><span>The difference is<br /><em>the story.</em></span></h2><p><span>Mirror is a Self-Narrative OS. The same event can lead to completely different emotions, decisions, and futures — not because of what happened, but because of how you interpreted it.</span></p><p><span>Tarot, I Ching, symbols, and archetypes are reflection mediums. The medium changes. <strong>The interpretation is always yours.</strong></span></p></div><div style={{ paddingTop:96 }}><p className="pull-quote"><span>"Most of us think we're trapped by reality. But often, we're trapped by our interpretation of it."</span></p></div></div></div></section>
      <section id="how"><div className="container"><div className="two-col reveal"><div><p className="eyebrow"><span>How It Works</span></p><h2><span>A quiet ritual<br /><em>of observation.</em></span></h2><p><span>Mirror is not a Tarot app. These are reflection mediums — surfaces that help you see what you already carry.</span></p><div className="medium-chips" style={{ marginTop:28 }}><span className="chip">Tarot</span><span className="chip">I Ching</span><span className="chip">Symbols</span><span className="chip">Archetypes</span><span className="chip">More soon</span></div></div><div className="flow"><div className="flow-step"><span className="flow-num">01</span><div><div className="flow-label"><span>Choose a Reflection Medium</span></div><div className="flow-desc"><span>Tarot, I Ching, symbols, archetypes, and more.</span></div></div></div><div className="flow-step"><span className="flow-num">02</span><div><div className="flow-label"><span>Draw</span></div><div className="flow-desc"><span>Let intuition guide the selection.</span></div></div></div><div className="flow-step"><span className="flow-num">03</span><div><div className="flow-label"><span>Describe</span></div><div className="flow-desc"><span>What do you notice first?<br />What feeling appears? What story do you see?</span></div></div></div><div className="flow-step"><span className="flow-num">04</span><div><div className="flow-label"><span>Reflect</span></div><div className="flow-desc"><span>Mirror combines your interpretation with traditional meanings. Not to predict the future. <strong style={{ color:'var(--ink)' }}>To reveal how you create meaning.</strong></span></div></div></div><div className="flow-step"><span className="flow-num">05</span><div><div className="flow-label"><span>Reveal Your Interpretation Lens</span></div><div className="flow-desc"><span>Recurring narratives become visible over time.</span></div></div></div></div></div></div></section>
      <section id="lens"><div className="container--wide"><div className="lens-intro reveal"><p className="eyebrow"><span>Interpretation Lens</span></p><h2><span>How you tend to<br /><em>read the world.</em></span></h2><p><span>Mirror doesn't archive sessions. It reveals the patterns in how you interpret reality — the recurring lenses you carry without noticing.</span></p></div><div className="lens-card-wrap reveal"><svg viewBox="0 0 560 560" xmlns="http://www.w3.org/2000/svg"><rect width="560" height="560" fill="#f6f3ee" /><text x="40" y="50" fontFamily="DM Mono,monospace" fontSize="10" fill="#9a9088" letterSpacing="2.5">INTERPRETATION LENS</text><text x="68" y="126" fontFamily="Cormorant Garamond,serif" fontSize="17" fill="#1c1916" fontStyle="italic">Uncertainty is often read as danger</text><text x="54" y="510" fontFamily="Cormorant Garamond,serif" fontSize="15" fill="#1c1916" fontStyle="italic">"You often describe change as something that happens to you, rather than something you create."</text></svg></div></div></section>
      <section id="founder"><div className="container reveal"><div className="two-col"><div><p className="eyebrow"><span>Why I'm Building This</span></p><div><p style={{ fontFamily:'var(--serif)', fontSize:20, fontWeight:300, lineHeight:1.8, color:'var(--ink)', marginBottom:24 }}><span>I built Mirror because I needed it myself.</span></p><p style={{ fontFamily:'var(--serif)', fontSize:20, fontWeight:300, lineHeight:1.8, color:'var(--ink)', marginBottom:24 }}><span>I wanted a way to observe not only what happened, but how I kept interpreting what happened.</span></p><p style={{ fontFamily:'var(--serif)', fontSize:20, fontWeight:300, lineHeight:1.8, color:'var(--ink)' }}><span>Mirror began as an attempt to build the tool I wished existed.</span></p></div><p style={{ fontFamily:'var(--serif)', fontSize:15, fontStyle:'italic', color:'var(--ink-muted)', marginTop:44 }}><span>— The Founder</span></p></div><div style={{ paddingTop:72 }}><p className="pull-quote"><span>Most tools helped me record.<br />Few helped me observe.</span></p></div></div></div></section>
      <section id="founding"><div className="container reveal"><p className="eyebrow"><span>Founding Member</span></p><h2><span>Become a<br /><em>Founding Member</em></span></h2><p style={{ maxWidth:520, marginTop:24, fontSize:17, lineHeight:1.85 }}><span>Mirror is still being built. You're joining the earliest version of a new way to understand yourself.</span></p><ul className="benefits"><li><span>Lifetime access to Mirror</span></li><li><span>Your personal Interpretation Lens</span></li><li><span>Early access to future reflection systems</span></li><li><span>A voice in what Mirror becomes</span></li><li><span>Build-in-public access</span></li></ul><p className="founding-price">$2</p><p className="founding-price-note"><span>EARLY ACCESS · LIFETIME INCLUDED</span></p><div style={{ display:'flex', flexDirection:'column', alignItems:'flex-start', gap:14 }}><a href="https://www.creem.io/payment/prod_6oltU4M6Pxwj6JFSQbVHZQ" className="btn-primary" target="_blank" rel="noreferrer"><span>Become a Founding Member</span><span className="btn-sub"><span>$2 Early Access</span></span></a><p style={{ fontSize:13, color:'var(--ink-muted)', margin:0 }}><span>Want to contribute more? <a href="https://www.creem.io/payment/prod_6oltU4M6Pxwj6JFSQbVHZQ" target="_blank" rel="noreferrer" style={{ color:'var(--ink-soft)', borderBottom:'1px solid var(--line)' }}>Choose your own amount.</a></span></p></div></div></section>
      <footer><div className="footer-inner"><span className="footer-logo">Mirror</span><span className="footer-note"><span>A Self-Narrative OS · Built in Public</span></span></div></footer>
    </>
  );
}

function MirrorFlow({ step, setStep, question, setQuestion, suggestion, setSuggestion, deck, setDeck, chosenCard, setChosenCard, selectedCardIndex, setSelectedCardIndex, observations, setObservations, isShuffling, setIsShuffling, shufflePhase, setShufflePhase, onBack }: {
  step: FlowStep; setStep: React.Dispatch<React.SetStateAction<FlowStep>>; question: string; setQuestion: React.Dispatch<React.SetStateAction<string>>; suggestion: string; setSuggestion: React.Dispatch<React.SetStateAction<string>>; deck: Card[]; setDeck: React.Dispatch<React.SetStateAction<Card[]>>; chosenCard: Card | null; setChosenCard: React.Dispatch<React.SetStateAction<Card | null>>; selectedCardIndex: number | null; setSelectedCardIndex: React.Dispatch<React.SetStateAction<number | null>>; observations: ObservationAnswers; setObservations: React.Dispatch<React.SetStateAction<ObservationAnswers>>; isShuffling: boolean; setIsShuffling: React.Dispatch<React.SetStateAction<boolean>>; shufflePhase: "idle" | "mix" | "settle"; setShufflePhase: React.Dispatch<React.SetStateAction<"idle" | "mix" | "settle">>; onBack: () => void;
}) {
  React.useEffect(() => { if (!deck.length) setDeck(shuffle(CARDS)); }, [deck.length, setDeck]);
  const reflection = chosenCard ? reflect(question, chosenCard, observations) : "";
  const startShuffle = () => {
    if (isShuffling) return;
    setIsShuffling(true);
    setShufflePhase("mix");
    setDeck(shuffle(CARDS));
    window.setTimeout(() => setDeck(shuffle(CARDS)), 280);
    window.setTimeout(() => setDeck(shuffle(CARDS)), 620);
    window.setTimeout(() => setShufflePhase("settle"), 1200);
    window.setTimeout(() => {
      setIsShuffling(false);
      setShufflePhase("idle");
      setStep("draw");
    }, 2000);
  };

  return (
    <main style={{ minHeight:'100vh', background:'var(--cream)', color:'var(--ink)', fontFamily:"Georgia, 'Times New Roman', serif", padding:24 }}>
      <style>{`.tarot-back { position:relative; border-radius:18px; overflow:hidden; background: linear-gradient(180deg, #2a2520 0%, #1a1714 100%); border:1px solid rgba(255,255,255,0.08); box-shadow:0 20px 50px rgba(28,25,22,0.18); cursor:pointer; } .tarot-back::before { content:''; position:absolute; inset:10px; border-radius:12px; border:1px solid rgba(255,255,255,0.12); } .tarot-back::after { content:'✦'; position:absolute; inset:0; display:grid; place-items:center; color:rgba(247,243,238,0.75); font-family:var(--mono); font-size:30px; } .card-image { width:100%; display:block; border-radius:18px; box-shadow:0 24px 50px rgba(28,25,22,0.16); } .shuffle-card { animation: deckFloat 2s ease-in-out infinite alternate; } .mix-0 { transform: translate(-130px, -70px) rotate(-14deg); } .mix-1 { transform: translate(-76px, 52px) rotate(6deg); } .mix-2 { transform: translate(-12px, -92px) rotate(-3deg); } .mix-3 { transform: translate(48px, 58px) rotate(12deg); } .mix-4 { transform: translate(112px, -34px) rotate(-10deg); } .settle-0 { transform: translate(-150px, -42px) rotate(-8deg); } .settle-1 { transform: translate(-80px, 28px) rotate(4deg); } .settle-2 { transform: translate(0px, -24px) rotate(0deg); } .settle-3 { transform: translate(82px, 34px) rotate(8deg); } .rest-0 { transform: translate(-114px, -54px) rotate(-8deg); } .rest-1 { transform: translate(-12px, -22px) rotate(2deg); } .rest-2 { transform: translate(96px, 18px) rotate(6deg); } @keyframes deckFloat { 0% { filter: blur(0px); } 50% { filter: blur(0.2px); } 100% { filter: blur(0px); } }`}</style>
      <div style={{ maxWidth: 980, margin:'0 auto', paddingTop: 40 }}>
        <button type="button" onClick={onBack} style={{ border:'none', background:'transparent', color:'var(--ink-muted)', cursor:'pointer', padding:0, marginBottom:24 }}>← Back to home</button>
        {step === 'ask' && <AskStep question={question} setQuestion={setQuestion} onBegin={() => { setSuggestion(question.trim() ? `It sounds like you're asking about ${question.trim().slice(0, 40)}...` : "It sounds like you're asking about what matters most right now."); setStep('shuffle'); }} />}
        {step === 'shuffle' && <ShuffleStep deck={deck} shufflePhase={shufflePhase} isShuffling={isShuffling} onShuffle={startShuffle} />}
        {step === 'draw' && <DrawStep deck={deck} selectedCardIndex={selectedCardIndex} onPick={(card, idx) => { setChosenCard(card); setSelectedCardIndex(idx); setStep('observe'); }} />}
        {step === 'observe' && chosenCard && <ObserveStep card={chosenCard} observation={observations} setObservation={setObservations} onContinue={() => setStep('reflect')} />}
        {step === 'reflect' && chosenCard && <ReflectStep question={question} suggestion={suggestion} card={chosenCard} observation={observations} reflection={reflection} onRestart={() => { setQuestion(''); setSuggestion(''); setChosenCard(null); setSelectedCardIndex(null); setObservations({ first:'', attention:'', associations:'' }); setStep('ask'); }} onShuffleAgain={() => { setStep('shuffle'); }} />}
      </div>
    </main>
  );
}

function AskStep({ question, setQuestion, onBegin }: { question: string; setQuestion: React.Dispatch<React.SetStateAction<string>>; onBegin: () => void }) { return <div><h1 style={{ fontSize:'clamp(48px, 8vw, 72px)', fontWeight:300, margin:'0 0 18px' }}>Enter Mirror</h1><p style={{ fontFamily:'var(--serif)', fontSize:22, fontStyle:'italic', color:'var(--ink-soft)', marginBottom:24 }}>What are you experiencing lately?</p><textarea value={question} onChange={(e) => setQuestion(e.target.value)} placeholder="What are you experiencing lately?" style={{ width:'100%', minHeight:180, border:'1px solid var(--line)', borderRadius:18, padding:18, fontFamily:'system-ui, sans-serif', fontSize:16, background:'rgba(255,255,255,0.6)', color:'var(--ink)', outline:'none', resize:'vertical' }} /><div style={{ marginTop:18 }}><button type="button" onClick={onBegin} style={{ border:'none', borderRadius:999, background:'var(--ink)', color:'var(--cream)', padding:'14px 22px', cursor:'pointer', fontSize:14, letterSpacing:'0.04em' }}>Begin Reflection</button></div></div>; }
function ShuffleStep({ deck, shufflePhase, isShuffling, onShuffle }: { deck: Card[]; shufflePhase: "idle" | "mix" | "settle"; isShuffling: boolean; onShuffle: () => void }) {
  return <div style={{ textAlign:'center', padding:'80px 0' }}><p className="eyebrow" style={{ justifyContent:'center' }}><span>Shuffle</span></p><h2>Shuffle the deck, then choose one card.</h2><p style={{ maxWidth:620, margin:'0 auto 30px' }}>Observe the motion. Let the deck feel alive before you choose.</p><div style={{ position:'relative', width:'100%', height: 240, margin:'24px auto 40px', maxWidth: 560 }}>{Array.from({ length: 10 }).map((_, i) => { const phaseClass = shufflePhase === 'mix' ? `mix-${i % 5}` : shufflePhase === 'settle' ? `settle-${i % 4}` : `rest-${i % 3}`; return <div key={`${shufflePhase}-${i}`} className={`tarot-back ${isShuffling ? 'shuffle-card' : ''} ${phaseClass}`} style={{ position:'absolute', left:'50%', top:'50%', width: 92, height: 146, marginLeft:-46, marginTop:-73, opacity: shufflePhase === 'mix' ? 1 : 0.96, zIndex: 10 - i }} />; })}</div><button type="button" onClick={onShuffle} disabled={isShuffling} style={{ border:'none', borderRadius:999, background:'var(--ink)', color:'var(--cream)', padding:'14px 22px', cursor:isShuffling ? 'default' : 'pointer', opacity:isShuffling ? 0.6 : 1 }}>{isShuffling ? 'Shuffling…' : 'Shuffle the deck'}</button>{shufflePhase === 'idle' && <p style={{ marginTop:18, color:'var(--ink-muted)' }}>Choose the card that draws your attention.</p>}</div>; }
function DrawStep({ deck, selectedCardIndex, onPick }: { deck: Card[]; selectedCardIndex: number | null; onPick: (card: Card, idx: number) => void }) { return <div><p className="eyebrow"><span>Choose</span></p><h2>Choose the card that draws your attention.</h2><p style={{ maxWidth:620 }}>Observe the pattern of your attention. Let the card choose you as much as you choose it.</p><div style={{ display:'grid', gridTemplateColumns:'repeat(auto-fit,minmax(92px,1fr))', gap:14, alignItems:'start' }}>{deck.map((card, idx) => <button key={`${card.name}-${idx}`} type="button" aria-label={`Choose a card`} onClick={() => onPick(card, idx)} className="tarot-back" style={{ width:'100%', aspectRatio:'2 / 3', opacity: selectedCardIndex === null || selectedCardIndex === idx ? 1 : 0.7, transform: selectedCardIndex === idx ? 'translateY(-4px) scale(1.02)' : 'none' }} />)}</div></div>; }
function ObserveStep({ card, observation, setObservation, onContinue }: { card: Card; observation: ObservationAnswers; setObservation: React.Dispatch<React.SetStateAction<ObservationAnswers>>; onContinue: () => void }) { return <div><p className="eyebrow"><span>Reveal</span></p><h2>{card.name}</h2><div style={{ display:'grid', gridTemplateColumns:'minmax(280px, 360px) 1fr', gap:24, alignItems:'start' }}><img src={card.imageUrl} alt={card.name} className="card-image" /><div><p style={{ fontFamily:'var(--serif)', fontStyle:'italic', fontSize:22, color:'var(--ink-soft)' }}>Observe before meaning.</p><label style={{ display:'block', marginBottom:14 }}>What did you notice first?<textarea value={observation.first} onChange={(e) => setObservation((v) => ({ ...v, first: e.target.value }))} style={inputStyle} /></label><label style={{ display:'block', marginBottom:14 }}>What caught your attention?<textarea value={observation.attention} onChange={(e) => setObservation((v) => ({ ...v, attention: e.target.value }))} style={inputStyle} /></label><label style={{ display:'block', marginBottom:14 }}>What associations came up?<textarea value={observation.associations} onChange={(e) => setObservation((v) => ({ ...v, associations: e.target.value }))} style={inputStyle} /></label><button type="button" onClick={onContinue} disabled={!(observation.first.trim() && observation.attention.trim() && observation.associations.trim())} style={{ border:'none', borderRadius:999, background:'var(--ink)', color:'var(--cream)', padding:'14px 22px', cursor:'pointer', opacity: observation.first.trim() && observation.attention.trim() && observation.associations.trim() ? 1 : 0.45 }}>Begin Reflection</button></div></div></div>; }
function ReflectStep({ question, suggestion, card, observation, reflection, onRestart, onShuffleAgain }: any) { return <div><p className="eyebrow"><span>Reflection</span></p><h2>Your Mirror</h2><div style={{ border:'1px solid var(--line)', borderRadius:18, background:'rgba(255,255,255,0.5)', padding:24 }}><p style={{ fontSize:14, color:'var(--ink-muted)' }}>This is not fortune telling or prediction. It is self-observation and narrative reflection.</p><p style={{ fontFamily:'var(--serif)', fontSize:20, fontStyle:'italic' }}>Question: {question || suggestion}</p><p style={{ fontFamily:'var(--serif)', fontSize:18 }}>Card: {card.name}</p><p style={{ whiteSpace:'pre-wrap', fontSize:18, lineHeight:1.8 }}>{reflection}</p></div><div style={{ marginTop:18, display:'flex', gap:12, flexWrap:'wrap' }}><button type="button" onClick={onShuffleAgain} style={{ border:'1px solid var(--line)', borderRadius:999, background:'transparent', color:'var(--ink)', padding:'14px 22px', cursor:'pointer' }}>Draw Again</button><button type="button" onClick={onRestart} style={{ border:'none', borderRadius:999, background:'var(--ink)', color:'var(--cream)', padding:'14px 22px', cursor:'pointer' }}>Start Over</button></div></div>; }
const inputStyle: React.CSSProperties = { width:'100%', minHeight:96, marginTop:8, border:'1px solid var(--line)', borderRadius:14, padding:14, fontFamily:'system-ui, sans-serif', fontSize:15, background:'rgba(255,255,255,0.6)', color:'var(--ink)', outline:'none', resize:'vertical' };
function reflect(question: string, card: Card, obs: ObservationAnswers) { return `This is not fortune telling or prediction.

Your question points toward: ${question || 'something unsaid'}.

The card ${card.name} suggests: ${card.meaning}

What you noticed first: ${obs.first}
What caught your attention: ${obs.attention}
What associations came up: ${obs.associations}

Mirror is asking you to notice the pattern of your own interpretation before rushing to a conclusion.`; }
function shuffle<T>(items: T[]): T[] { return [...items].sort(() => Math.random() - 0.5); }
ReactDOM.createRoot(document.getElementById('root')!).render(<React.StrictMode><App /></React.StrictMode>);
